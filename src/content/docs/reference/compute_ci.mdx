---
title: "Compute confidence interval for parameter estimates"
description: "Calculates confidence intervals using the Wald method with optional back-transformation."
pagefind: true
---

## Description

Calculates confidence intervals using the Wald method with optional back-transformation. For LogNormal transform, the CI is computed on the log scale and then exponentiated. For other transforms, standard symmetric intervals are computed.

## Usage

```r
compute_ci(estimate, se, ci_level = 0.95, transform = "identity")
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>estimate</code></td>
<td>The parameter estimate(s), can be a vector</td>
</tr>
<tr>
<td><code>se</code></td>
<td>The standard error(s) of the estimate(s), can be a vector</td>
</tr>
<tr>
<td><code>ci_level</code></td>
<td>Confidence level between 0 and 1 (e.g., 0.95 for 95% CI). Defaults to 0.95.</td>
</tr>
<tr>
<td><code>transform</code></td>
<td>Transformation type(s), can be a vector: "LogNormal", "AddErr", "Proportional", or "Identity". Defaults to "Identity".</td>
</tr>
</tbody>
</table>
</div>

## Value

A list with `lower` and `upper` vectors for the CI bounds

## Examples

```r
## Not run: 
compute_ci(1.5, 0.2)$lower
df %>% mutate(
  ci_lower = compute_ci(estimate, se)$lower,
  ci_upper = compute_ci(estimate, se)$upper
)

## End(Not run)
```
