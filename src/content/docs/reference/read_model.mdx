---
title: "Read a NONMEM model from a .mod or .ctl file"
description: "Read a NONMEM model from a. mod or. ctl file."
sidebar:
  label: "read_model"
pagefind: true
---

## Description

Read a NONMEM model from a .mod or .ctl file






## Usage

```r
read_model(path)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr><th>Argument</th><th>Description</th></tr>
</thead>
<tbody>
<tr>
<td><code>path</code></td>
<td>path to a .mod or .ctl file.</td>
</tr>
</tbody>
</table>
</div>


## Value

A `hyperion_nonmem_model` S3 object with attributes:
- `filename`: the model stem (e.g. `"run001"`)
- `model_source`: path to the source file, relative to the pharos config dir
- `run_status`: `"run"`, `"running"`, or `"not_run"` determined from output files on disk



## Examples

```r
# Not run:
read_model("model/nonmem/run001.mod")
```

