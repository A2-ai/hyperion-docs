---
title: "Apply lookup defaults to a ModelComments object"
description: "Convenience function to apply lookup defaults to all comments in a ModelComments object (theta, omega, sigma)."
pagefind: true
---

## Description

Convenience function to apply lookup defaults to all comments in a ModelComments object (theta, omega, sigma).

## Usage

```r
apply_lookup(info, lookup_path)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>info</code></td>
<td>A ModelComments object</td>
</tr>
<tr>
<td><code>lookup_path</code></td>
<td>Path to a toml lookup file</td>
</tr>
</tbody>
</table>
</div>

## Value

The modified ModelComments object
