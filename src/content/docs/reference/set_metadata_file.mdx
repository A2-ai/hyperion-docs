---
title: "Creates a metadata file for a NONMEM model"
description: "This function creates a metadata file that stores information about a NONMEM model, including its description, tags, and lineage information."
pagefind: true
---

## Description

This function creates a metadata file that stores information about a NONMEM model, including its description, tags, and lineage information. The metadata is stored in a structured format that can be used for model tracking and documentation.

## Usage

```r
set_metadata_file(model_path, description = NULL, tags = NULL, based_on = NULL)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>model_path</code></td>
<td>Path to the NONMEM model file (required)</td>
</tr>
<tr>
<td><code>description</code></td>
<td>Optional description of the model and its purpose</td>
</tr>
<tr>
<td><code>tags</code></td>
<td>Character vector of tags to categorize or label the model</td>
</tr>
<tr>
<td><code>based_on</code></td>
<td>Character vector of model names/paths that this model is based on</td>
</tr>
</tbody>
</table>
</div>

## Value

Returns invisibly after creating the metadata file

## Examples

```r
## Not run: 
# Create basic metadata for a model
create_metadata_file("run001.mod", description = "Base population PK model")

# Create metadata with tags and lineage
create_metadata_file(
  "run002.mod",
  description = "PK model with covariate effects",
  tags = c("population", "pk", "covariates"),
  based_on = c("run001.mod")
)

# Overwrite existing metadata
create_metadata_file(
  "run001.mod",
  description = "Updated base model",
  overwrite = TRUE
)

## End(Not run)
```
