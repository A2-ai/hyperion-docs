---
title: "Gets parameter names from model using typed comment parsing (internal)"
description: "This function extracts parameter names using the pharos typed comment parser."
pagefind: true
---

## Description

This function extracts parameter names using the pharos typed comment parser. For general use, prefer the R-side get_parameter_names() which handles both typed and raw comment formats.

## Usage

```r
get_model_parameter_names(model)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>model</code></td>
<td>hyperion_nonmem_model object from read_model()</td>
</tr>
</tbody>
</table>
</div>

## Value

Named character vector with NONMEM names as names and user-friendly names as values
