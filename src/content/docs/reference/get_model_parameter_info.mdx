---
title: "Extract all parameter comments from a model as ModelComments object"
description: "Parses parameter comments and returns structured metadata for theta, omega, and sigma parameters."
sidebar:
  label: "get_model_parameter_info"
pagefind: true
---

## Description

Parses parameter comments and returns structured metadata for theta, omega,
and sigma parameters.

## Details

For model objects sourced from `.mod`/`.ctl` files:
- if run status is `"run"`, metadata is read from the corresponding `.lst`
- otherwise (`"not_run"`/`"running"`), metadata is read from the model file






## Usage

```r
get_model_parameter_info(mod, lookup_path = NULL)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr><th>Argument</th><th>Description</th></tr>
</thead>
<tbody>
<tr>
<td><code>mod</code></td>
<td>A hyperion_nonmem_model object or path to a run output directory containing an .lst file.</td>
</tr>
<tr>
<td><code>lookup_path</code></td>
<td>Optional path to a TOML lookup file. If provided, fills NULL fields (display, description, unit, parameterization) from the lookup.</td>
</tr>
</tbody>
</table>
</div>


## Value

A `ModelComments` object containing theta, omega, and sigma comments.




## Comment Parsing

Comments are parsed by pharos according to the `[nonmem.comments]` section
of `pharos.toml`. Set `type = "type1"` for strict structured comments, or
`type = "type2"` for a more flexible structured grammar. See pharos
documentation for accepted formats.



## See Also

[`get_parameter_transform()`](../get_parameter_transform/), [`get_theta_names()`](../get_theta_names/), [`get_comment()`](../get_comment/)


